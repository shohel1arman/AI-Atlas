/* Deep dive — Data Analysis (SQL, statistics & pandas) */
window.AtlasRef && window.AtlasRef.register('data-analysis', {
  color: '#2EC4DE',
  title: 'From raw tables to real answers',
  lead: 'The working analyst\'s toolkit — the relational model and SQL that pull data out, the statistics that describe it honestly, the cleaning and encoding that make it modelling-ready, and the pandas that ties it all together. Every topic with intuition, exact rules, worked queries and runnable code in both SQL and pandas.',
  sections: [

    { h: 'The relational model: tables, rows, keys', p: [
      'Almost all business data lives in <b>relations</b> — what we call tables. A <b>row</b> (tuple) is one observation; a <b>column</b> (attribute) is one field with a fixed type. The power of the model is that tables are connected by <b>keys</b> rather than by nesting, so any two facts can be joined on demand.',
      'A <b>primary key</b> is a column (or set) that uniquely identifies each row — it must be unique and non-null. A <b>foreign key</b> is a column that references the primary key of another table, encoding a relationship (e.g. <code>orders.customer_id</code> points at <code>customers.id</code>). <b>Normalization</b> is the discipline of storing each fact exactly once so it cannot disagree with itself.'],
      terms: [['Primary key', 'unique, non-null identifier for a row.'], ['Foreign key', 'a column referencing another table\'s primary key.'], ['Cardinality', 'how rows relate: one-to-one, one-to-many, many-to-many.'], ['NULL', 'the marker for "unknown / missing" — not zero, not empty string.'], ['Normalization', 'each fact stored once to avoid update anomalies.'], ['Schema', 'the set of tables, columns, types and constraints.']],
      ex: [{ h: 'A tiny two-table schema', p: ['A <code>customers</code> table holds one row per person; an <code>orders</code> table holds many rows per customer, each carrying a <code>customer_id</code> foreign key. This one-to-many link lets us count orders per customer without duplicating names.'],
        code: 'CREATE TABLE customers (\n    id      INTEGER PRIMARY KEY,\n    name    TEXT NOT NULL,\n    country TEXT\n);\n\nCREATE TABLE orders (\n    id          INTEGER PRIMARY KEY,\n    customer_id INTEGER REFERENCES customers(id),  -- foreign key\n    amount      NUMERIC,\n    created_at  DATE\n);' }],
      note: 'NULL is contagious: any comparison with NULL yields NULL (not TRUE), which is why you must write <code>WHERE col IS NULL</code>, never <code>WHERE col = NULL</code>.' },

    { h: 'SELECT, WHERE, ORDER BY & the logical order of execution', p: [
      'The <b>SELECT</b> statement is a declarative request: you describe the rows you want, not how to fetch them. <b>SELECT</b> chooses columns, <b>WHERE</b> filters rows by a boolean condition, <b>ORDER BY</b> sorts (ascending by default; add <code>DESC</code>), and <b>LIMIT</b> caps how many rows return. Filters combine with <code>AND</code>/<code>OR</code>/<code>NOT</code> and use operators like <code>BETWEEN</code>, <code>IN (...)</code>, <code>LIKE \'%pat%\'</code> and <code>IS NULL</code>.',
      'Crucially, SQL is written <code>SELECT ... FROM ... WHERE ...</code> but <b>not evaluated in that order</b>. The engine follows a logical pipeline, and knowing it explains most beginner errors — why a <code>SELECT</code> alias cannot be used in <code>WHERE</code>, and why <code>WHERE</code> cannot filter on an aggregate. The order is <b>FROM</b> (assemble rows, run JOINs), <b>WHERE</b> (filter rows), <b>GROUP BY</b> (collapse into groups), <b>HAVING</b> (filter groups), <b>SELECT</b> (compute output columns and aliases), <b>ORDER BY</b> (sort), then <b>LIMIT</b> (trim).'],
      eqn: 'FROM  ->  WHERE  ->  GROUP BY  ->  HAVING  ->  SELECT  ->  ORDER BY  ->  LIMIT',
      ex: [{ h: 'Top 5 recent large UK orders', p: ['Find UK orders over 100, newest first, keeping only five. Read it as English: from orders, where these conditions hold, sorted this way, show five.'],
        code: 'SELECT id, customer_id, amount, created_at\nFROM orders\nWHERE amount > 100\n  AND created_at >= DATE \'2026-01-01\'\nORDER BY created_at DESC\nLIMIT 5;' },
      { h: 'Why HAVING, not WHERE', p: ['To keep only customers with more than 3 orders, the count is a per-group value that does not exist until after <code>GROUP BY</code> — so the filter must live in <code>HAVING</code>. Writing <code>WHERE COUNT(*) &gt; 3</code> errors because <code>WHERE</code> runs first, before any grouping.'],
        code: 'SELECT customer_id, COUNT(*) AS n_orders\nFROM orders\nWHERE amount > 0          -- filters rows (runs first)\nGROUP BY customer_id\nHAVING COUNT(*) > 3       -- filters groups (runs after grouping)\nORDER BY n_orders DESC;' }],
      note: 'Develop queries against a <code>LIMIT</code>ed sample; only remove the limit once the logic is right, to avoid scanning huge tables while iterating.' },

    { h: 'GROUP BY & aggregate functions', p: [
      '<b>GROUP BY</b> partitions rows into buckets that share the same key values, then an <b>aggregate</b> collapses each bucket to a single number. The five core aggregates are <b>COUNT</b> (how many), <b>SUM</b> (total), <b>AVG</b> (mean), <b>MIN</b> and <b>MAX</b>. Every column in <code>SELECT</code> must either appear in <code>GROUP BY</code> or be inside an aggregate.',
      'Aggregates <b>ignore NULLs</b> (except <code>COUNT(*)</code>, which counts rows regardless). <code>COUNT(col)</code> counts non-null values; <code>COUNT(DISTINCT col)</code> counts unique values. This is a frequent source of surprise when a column has missing data.'],
      table: { cols: ['Function', 'Returns', 'NULL handling'], rows: [
        ['<b>COUNT(*)</b>', 'number of rows in the group', 'counts every row'],
        ['<b>COUNT(col)</b>', 'non-null values of col', 'skips NULLs'],
        ['<b>SUM(col)</b>', 'total of col', 'skips NULLs'],
        ['<b>AVG(col)</b>', 'mean = SUM/COUNT of non-nulls', 'skips NULLs (does not treat them as 0)'],
        ['<b>MIN / MAX</b>', 'smallest / largest value', 'skips NULLs']] },
      ex: [{ h: 'Revenue summary per country', p: ['Group orders by their customer\'s country (via a join) and report order count, total and average spend. Sorting by total revenue surfaces the biggest markets.'],
        code: 'SELECT c.country,\n       COUNT(*)        AS n_orders,\n       SUM(o.amount)   AS revenue,\n       ROUND(AVG(o.amount), 2) AS avg_order\nFROM orders o\nJOIN customers c ON c.id = o.customer_id\nGROUP BY c.country\nORDER BY revenue DESC;' }] },

    { h: 'JOINs: INNER, LEFT, RIGHT, FULL', p: [
      'A <b>JOIN</b> combines rows from two tables by matching a condition, usually a foreign-key equality. The join <b>type</b> decides what happens to rows that have no match on the other side — the single most important thing to get right, because the wrong type silently drops or duplicates rows.',
      '<b>INNER JOIN</b> keeps only matched pairs. <b>LEFT JOIN</b> keeps every left row, filling right-side columns with NULL when there is no match (perfect for "customers and their orders, including those with none"). <b>RIGHT JOIN</b> is the mirror image, and <b>FULL OUTER JOIN</b> keeps unmatched rows from both sides. A missing/mistyped join key produces an accidental many-to-many blow-up called a <b>fan-out</b>.'],
      table: { cols: ['Join', 'Keeps', 'Typical use'], rows: [
        ['<b>INNER</b>', 'rows matched on both sides', 'orders that definitely have a customer'],
        ['<b>LEFT</b>', 'all left rows + matches', 'all customers, even with zero orders'],
        ['<b>RIGHT</b>', 'all right rows + matches', 'rare; usually rewritten as LEFT'],
        ['<b>FULL OUTER</b>', 'all rows from both sides', 'reconciling two lists to find gaps']] },
      ex: [{ h: 'Find customers who never ordered', p: ['A <code>LEFT JOIN</code> keeps every customer; those with no order get NULL in the order columns. Filtering <code>WHERE o.id IS NULL</code> isolates exactly the non-buyers — the classic "anti-join" pattern.'],
        code: 'SELECT c.id, c.name\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id\nWHERE o.id IS NULL;   -- kept only customers with no matching order' }],
      note: 'An INNER JOIN here would hide the very rows you want, because non-buyers have nothing to match. Choose LEFT whenever "rows with no match" is part of the answer.' },

    { h: 'Window functions: rank & running totals', p: [
      'A <b>window function</b> computes across a set of rows <b>related to the current row</b> without collapsing them — unlike <code>GROUP BY</code>, every input row survives and gains an extra computed column. The <code>OVER (...)</code> clause defines the window: <code>PARTITION BY</code> splits into independent groups and <code>ORDER BY</code> sequences rows within each group.',
      'Ranking functions: <b>ROW_NUMBER()</b> gives a strict 1,2,3 with no ties; <b>RANK()</b> gives ties the same rank then skips (1,1,3); <b>DENSE_RANK()</b> does not skip (1,1,2). A <b>running total</b> is a windowed <code>SUM(...) OVER (ORDER BY ...)</code>, which accumulates as it walks the ordered rows. These power "top-N per group", cumulative revenue, and period-over-period change (via <code>LAG</code>/<code>LEAD</code>).'],
      ex: [{ h: 'Each customer\'s most recent order', p: ['Number every order within a customer newest-first, then keep row number 1. This "top-1 per group" job is awkward with plain SQL but trivial with <code>ROW_NUMBER()</code>.'],
        code: 'WITH ranked AS (\n  SELECT o.*,\n         ROW_NUMBER() OVER (PARTITION BY customer_id\n                            ORDER BY created_at DESC) AS rn\n  FROM orders o\n)\nSELECT * FROM ranked WHERE rn = 1;' },
      { h: 'Cumulative revenue over time', p: ['A running total sums <code>amount</code> in date order, so each row shows revenue-to-date. Removing the frame would sum the whole partition instead.'],
        code: 'SELECT created_at, amount,\n       SUM(amount) OVER (ORDER BY created_at\n                         ROWS UNBOUNDED PRECEDING) AS running_total\nFROM orders\nORDER BY created_at;' }] },

    { h: 'CTEs & subqueries', p: [
      'A <b>subquery</b> is a query nested inside another — in the <code>FROM</code> clause (a derived table), in <code>WHERE</code> (e.g. <code>IN</code> / <code>EXISTS</code>), or in <code>SELECT</code> (a scalar value). A <b>Common Table Expression (CTE)</b>, written <code>WITH name AS (...)</code>, is a named subquery placed up front; it reads top-to-bottom, can be referenced multiple times, and can chain, which makes complex logic far more legible than deep nesting.',
      'Prefer CTEs for readability: each step gets a name, so a five-stage transformation reads like a paragraph. Use <b>correlated subqueries</b> (which reference the outer row) sparingly — <code>EXISTS</code> is often clearer and faster than <code>IN</code> for "does a related row exist?". Postgres also supports <b>recursive</b> CTEs for hierarchies and graphs.'],
      ex: [{ h: 'Above-average orders, two ways', p: ['A scalar subquery computes the global mean once and each row is compared to it. The same idea expressed as a CTE names the threshold, which pays off as logic grows.'],
        code: '-- scalar subquery\nSELECT * FROM orders\nWHERE amount > (SELECT AVG(amount) FROM orders);\n\n-- same, as a CTE\nWITH stats AS (SELECT AVG(amount) AS mean_amt FROM orders)\nSELECT o.*\nFROM orders o, stats\nWHERE o.amount > stats.mean_amt;' }],
      note: 'EXISTS short-circuits on the first match and handles NULLs gracefully, whereas <code>NOT IN (subquery)</code> returns nothing if the subquery yields a single NULL — a notorious bug.' },

    { h: 'The exploratory data analysis (EDA) workflow', p: [
      '<b>Exploratory Data Analysis</b> is the deliberate first pass over a fresh dataset, before any modelling: understand shape, quality and structure so later conclusions are trustworthy. It is iterative and question-driven, not a fixed recipe, but a reliable sequence keeps you honest.',
      'A practical loop: (1) <b>profile</b> — rows, columns, dtypes, memory; (2) <b>quality</b> — missingness, duplicates, impossible values; (3) <b>univariate</b> — distribution of each variable (histograms, value counts); (4) <b>bivariate</b> — relationships (scatter, correlation, group summaries); (5) <b>hypotheses</b> — form and check hunches; (6) <b>document</b> — record decisions and surprises. The goal is not pretty charts but a mental model of the data and a list of cleaning tasks.'],
      terms: [['Profiling', 'shape, types, ranges and memory footprint at a glance.'], ['Univariate', 'looking at one variable\'s distribution in isolation.'], ['Bivariate', 'looking at how two variables move together.'], ['Data dictionary', 'documented meaning, units and valid range of each column.']],
      ex: [{ h: 'First five minutes with a DataFrame', p: ['These calls answer "what am I holding?" — shape and types, a numeric summary, missing counts, and category frequencies. Run them before writing any analysis code.'],
        code: 'import pandas as pd\ndf = pd.read_csv(\'orders.csv\')\n\ndf.shape                    # (rows, cols)\ndf.info()                   # dtypes + non-null counts\ndf.describe(include=\'all\')  # numeric + categorical summary\ndf.isna().mean().sort_values(ascending=False)  # % missing per col\ndf[\'country\'].value_counts(dropna=False)        # category frequencies\ndf.duplicated().sum()        # exact duplicate rows' }] },

    { h: 'Descriptive statistics, distributions & outliers', p: [
      '<b>Central tendency</b> asks "what is typical?". The <b>mean</b> is the balance point but is pulled by outliers; the <b>median</b> (50th percentile) is robust and better for skewed data like income; the <b>mode</b> is the most frequent value and the only option for categories. <b>Spread</b> asks "how variable?": <b>variance</b> is the mean squared deviation and <b>standard deviation</b> is its square root, back in the original units. <b>Quantiles</b> cut sorted data into equal parts (quartiles Q1/Q2/Q3, percentiles); <b>IQR = Q3 - Q1</b> is a robust spread. <b>Skewness</b> measures asymmetry (positive = long right tail) and <b>kurtosis</b> measures tail heaviness.',
      'The <b>shape</b> of the distribution decides which methods are valid — many assume roughly <b>normal</b> (bell-shaped) data, yet real data is often right-skewed (counts, prices) or multi-modal, so always plot a histogram before trusting a mean. An <b>outlier</b> sits far from the bulk — an error to fix or a rare real event to keep, never deleted blindly. Two standard detectors: the <b>IQR rule</b> flags points outside [Q1 - 1.5*IQR, Q3 + 1.5*IQR] (robust, distribution-free, the basis of box plots), and the <b>z-score</b> counts standard deviations from the mean (|z| &gt; 3 is a common flag, but z-scores are distorted by the very outliers they hunt, so prefer IQR for heavy tails).'],
      eqn: 'mean  m = (1/n) Sigma x_i\nvariance  s^2 = (1/(n-1)) Sigma (x_i - m)^2        std s = sqrt(s^2)\nz = (x - m) / s        IQR fence: [ Q1 - 1.5*IQR , Q3 + 1.5*IQR ]',
      ex: [{ h: 'Mean vs median on skewed pay', p: ['Nine staff earn ~50k and the founder earns 5M. The mean (~545k) describes nobody, while the median (50k) captures the typical employee. Choosing the wrong statistic here would badly mislead.'],
        code: 'import numpy as np\nfrom scipy import stats\npay = np.array([48,50,50,51,52,49,50,53,50, 5000])  # in thousands\npay.mean()          # ~545.3  (dragged up by the outlier)\nnp.median(pay)      # 50.0    (robust, "typical")\nstats.skew(pay), stats.kurtosis(pay)  # large positive, heavy tail' },
      { h: 'Flagging outliers with the IQR fence', p: ['Compute the quartiles, build the fence, and mark anything outside it — exactly what a box plot\'s whiskers and dots show. A robust, assumption-free first screen.'],
        code: 'q1, q3 = df[\'amount\'].quantile([0.25, 0.75])\niqr = q3 - q1\nlo, hi = q1 - 1.5*iqr, q3 + 1.5*iqr\noutliers = df[(df[\'amount\'] < lo) | (df[\'amount\'] > hi)]\nprint(len(outliers), \'outliers\', \'fence:\', round(lo,1), round(hi,1))' }],
      note: 'Log-transforming a right-skewed variable often turns it roughly symmetric, stabilising both statistics and models — a cheap, common fix. Report a robust pair (median + IQR) alongside mean + std whenever data is skewed.' },

    { h: 'Correlation vs causation', p: [
      '<b>Correlation</b> quantifies how two variables move together. <b>Pearson\'s r</b> (in [-1, 1]) measures <i>linear</i> association; <b>Spearman\'s rho</b> uses ranks and captures any monotonic relationship, robust to outliers and non-linearity. A correlation near 0 means no <i>linear</i> link — the variables can still be strongly related in a curved way, which is why you also plot.',
      'The cardinal rule: <b>correlation does not imply causation</b>. Two series can correlate because one causes the other, because a hidden <b>confounder</b> drives both (ice-cream sales and drownings both rise with summer heat), because of reverse causation, or by pure chance in a small sample. Establishing cause needs a controlled experiment (A/B test) or careful causal inference, not a correlation coefficient.'],
      eqn: 'r = cov(X,Y) / (s_X * s_Y)  in  [-1, 1]',
      ex: [{ h: 'A spurious correlation', p: ['Across cities, number of firefighters correlates strongly with fire damage. Firefighters do not cause damage; big fires (the confounder) summon many firefighters and cause much damage. Conditioning on fire size makes the correlation vanish.'],
        code: 'import pandas as pd\ndf[[\'firefighters\',\'damage\',\'fire_size\']].corr(method=\'pearson\')\n# firefighters~damage: strong +\n# but within a fixed fire_size the link disappears:\ndf.groupby(pd.qcut(df[\'fire_size\'], 4))[[\'firefighters\',\'damage\']].corr()' }],
      note: 'Anscombe\'s quartet: four datasets with identical means, variances and r=0.82 look completely different when plotted. Never report a correlation without seeing the scatter.' },

    { h: 'Missing data, types, cleaning, scaling & encoding', p: [
      'Why data is missing determines what you may do. <b>MCAR</b> (Missing Completely At Random): unrelated to anything — dropping rows is unbiased but wasteful. <b>MAR</b> (Missing At Random): missingness depends on <i>observed</i> variables (older users skip a field) — imputation using those variables is valid. <b>MNAR</b> (Missing Not At Random): missingness depends on the <i>unobserved</i> value itself (high earners hide income) — the hardest case, where naive imputation biases results. Tactics: <b>drop</b> when small and MCAR; otherwise <b>impute</b> (median for skewed numerics, a "Missing" category for categoricals) and add a <b>missing-indicator</b> flag so a model can learn whether absence is itself informative.',
      'Getting <b>types</b> right is half of cleaning: numeric, categorical (nominal vs ordinal), datetime and boolean each unlock different operations. Typical cleaning: trim/normalise case and whitespace, parse dates, coerce numeric strings, de-duplicate, and reconcile inconsistent categories ("USA" vs "U.S."). Then most models need numeric <b>scaling</b> so no feature dominates by unit alone: <b>standardization</b> (z-score) centres to mean 0, std 1 (the default); <b>normalization</b> (min-max) squashes to [0,1] (good for bounded inputs and neural nets). Categoricals need <b>encoding</b>: <b>one-hot</b> for low-cardinality nominal, <b>label/ordinal</b> when order is real, and <b>target/frequency</b> for high-cardinality — fit on train only.'],
      eqn: 'standardize:  z = (x - mean) / std\nnormalize (min-max):  x\' = (x - min) / (max - min)',
      table: { cols: ['Mechanism / Encoding', 'Meaning', 'Safe approach / caution'], rows: [
        ['<b>MCAR</b>', 'missing depends on nothing', 'drop rows, or simple impute'],
        ['<b>MAR</b>', 'depends on observed columns', 'conditional / model-based imputation'],
        ['<b>MNAR</b>', 'depends on the missing value', 'model the missingness; add indicator'],
        ['<b>One-hot</b>', 'nominal, few categories', 'dimension blow-up if high-cardinality'],
        ['<b>Label / ordinal</b>', 'genuine order (low<med<high)', 'implies false order if nominal'],
        ['<b>Target / frequency</b>', 'high-cardinality categories', 'leakage — fit on train, smooth rare levels']] },
      ex: [{ h: 'Median impute + a missingness flag', p: ['Record where a value was missing, then fill numeric gaps with the median (robust to skew). The flag lets a downstream model exploit the pattern of absence rather than pretending it was signal.'],
        code: 'df[\'age_missing\'] = df[\'age\'].isna().astype(int)   # keep the signal\ndf[\'age\'] = df[\'age\'].fillna(df[\'age\'].median())  # robust fill\ndf[\'country\'] = df[\'country\'].fillna(\'Unknown\')    # missing = its own level' },
      { h: 'Scale numerics, one-hot the categories', p: ['Fit the scaler and encoder on training data only, then apply to test data with the same parameters — fitting on the whole set leaks information from test into train and inflates your scores.'],
        code: 'from sklearn.preprocessing import StandardScaler\nscaler = StandardScaler().fit(train[[\'age\',\'amount\']])   # fit on TRAIN\ntrain[[\'age\',\'amount\']] = scaler.transform(train[[\'age\',\'amount\']])\ntest[[\'age\',\'amount\']]  = scaler.transform(test[[\'age\',\'amount\']])\ntrain = pd.get_dummies(train, columns=[\'country\'], drop_first=True)' }],
      note: 'Fit any transformation (scaler, encoder, imputer) on the training split only. Fitting on all data is <b>data leakage</b> — the most common reason a model looks great offline yet fails in production.' },

    { h: 'Visualization: grammar & common pitfalls', p: [
      'A chart is an <b>encoding</b>: data values mapped to visual channels (position, length, colour, area). Human perception reads <b>position and length</b> most accurately and <b>area, angle and colour</b> least, which is why bar and line charts beat pie and bubble charts for precise comparison. Pick the chart from the <b>question</b>, not from novelty — the right default is usually the simplest one that answers it.',
      'The same data can inform or deceive. Frequent traps: a <b>truncated y-axis</b> that starts above zero and exaggerates tiny differences (bars must start at zero); <b>dual y-axes</b> that manufacture a fake correlation; <b>3-D and pie charts</b> that distort areas and angles; <b>rainbow colour maps</b> that invent false boundaries and fail for colour-blind readers (use viridis); and <b>overplotting</b>, where thousands of points merge into a blob. Deeper hazards are <b>cherry-picked ranges</b> and <b>Simpson\'s paradox</b>, where a trend in every subgroup reverses once the groups are pooled — always check whether a headline relationship survives disaggregation.'],
      table: { cols: ['Question', 'Best chart', 'Why'], rows: [
        ['Compare categories', '<b>Bar chart</b>', 'length read precisely; sort by value'],
        ['Trend over time', '<b>Line chart</b>', 'position + connection show change'],
        ['Distribution of one variable', '<b>Histogram / box plot</b>', 'reveals shape, spread, outliers'],
        ['Relationship of two numerics', '<b>Scatter plot</b>', 'shows correlation, clusters, outliers'],
        ['Part of a whole (few parts)', '<b>Stacked bar</b>', 'more precise than a pie'],
        ['Dense matrix of values', '<b>Heatmap</b>', 'colour encodes magnitude across a grid']] },
      ex: [{ h: 'Overplotting, and the honest fix', p: ['Fifty thousand raw points overwhelm a scatter into a solid mass. Alpha transparency plus a density (hexbin) view restores the real structure — where points actually concentrate — without deleting data.'],
        code: 'import matplotlib.pyplot as plt\nfig, (a1, a2) = plt.subplots(1, 2, figsize=(10,4))\na1.scatter(df.x, df.y, s=6, alpha=0.05)   # transparency reveals density\na2.hexbin(df.x, df.y, gridsize=40, cmap=\'viridis\')  # 2-D density\nplt.tight_layout()' }],
      note: 'Before publishing a chart, ask: does the axis start at zero where it should, is one colour scale doing one job, and would a subgroup breakdown change the story?' },

    { h: 'pandas essentials: filter, groupby, merge, pivot', p: [
      '<b>pandas</b> is SQL-on-a-laptop plus everything around it. The DataFrame is a table; the same four verbs recur. <b>Filter</b> with boolean masks (<code>df[df.amount &gt; 100]</code>) or the readable <code>query()</code>. <b>groupby-aggregate</b> mirrors SQL\'s <code>GROUP BY</code> and accepts named aggregations for tidy output. <b>merge</b> is JOIN, with <code>how=</code> choosing inner/left/right/outer. <b>pivot_table</b> reshapes long data into a cross-tab (a spreadsheet pivot), and <code>melt</code> reverses it.',
      'Two habits keep pandas fast and correct: prefer <b>vectorised</b> operations over Python loops (10-100x faster), and use <code>.loc[]</code> for explicit, label-based assignment to avoid the notorious <b>SettingWithCopy</b> warning that signals you edited a view, not the frame.'],
      ex: [{ h: 'The four verbs end to end', p: ['Filter to valid orders, summarise revenue and order count per country, then join customer metadata and pivot country-by-month revenue into a grid — the same pipeline you would write in SQL, in a handful of lines.'],
        code: 'import pandas as pd\n\n# 1) filter\nvalid = orders[orders[\'amount\'] > 0]\n\n# 2) groupby + named aggregates  (= GROUP BY)\nby_country = (valid.groupby(\'country\')\n                   .agg(revenue=(\'amount\',\'sum\'),\n                        n_orders=(\'id\',\'count\'),\n                        avg_order=(\'amount\',\'mean\'))\n                   .reset_index())\n\n# 3) merge  (= JOIN)\nenriched = valid.merge(customers, left_on=\'customer_id\',\n                       right_on=\'id\', how=\'left\')\n\n# 4) pivot to a cross-tab\ngrid = valid.pivot_table(index=\'country\', columns=\'month\',\n                         values=\'amount\', aggfunc=\'sum\', fill_value=0)' }],
      note: 'When a groupby-merge chain gets long, method chaining inside parentheses reads top-to-bottom like a CTE pipeline and avoids leaking intermediate variables.' },
  ]
});
