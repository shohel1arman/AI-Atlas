/* Deep dive — Machine Learning */
window.AtlasRef && window.AtlasRef.register('machine-learning', {
  color: '#22D3EE',
  title: 'Classical machine learning, end to end',
  lead: 'Before deep nets there was — and still is — a toolbox of models that win on tabular data every day: linear and logistic regression, trees and forests, SVMs, k-NN, Naive Bayes, clustering and dimensionality reduction. This is the full course: the paradigms, the math, worked numbers, and scikit-learn code, plus the evaluation and validation discipline that keeps you honest.',
  sections: [

    { h: 'The three learning paradigms', p: [
      'Machine learning splits by <b>what signal supervises it</b>. In <b>supervised learning</b> every example carries a label <code>(x, y)</code> and the model learns a mapping x&rarr;y — regression when y is continuous (house price), classification when y is a class (spam / not-spam). In <b>unsupervised learning</b> there are no labels; the model finds structure — clusters, low-dimensional manifolds, density. In <b>reinforcement learning</b> an agent takes actions in an environment and learns from a scalar <i>reward</i> signal, optimising long-run return rather than fitting fixed targets.',
      'Most business value today is supervised, because labels — however expensive — turn a vague goal into a measurable objective. Unsupervised methods are the workhorses of exploration, compression and feature building; RL dominates control, games and increasingly the fine-tuning of large language models (RLHF).'],
      terms: [['Supervised', 'learn x&rarr;y from labelled pairs; regression or classification.'], ['Unsupervised', 'find structure in unlabelled x: clustering, density, dimensionality reduction.'], ['Reinforcement', 'learn a policy from reward by trial and interaction over time.'], ['Semi-supervised', 'a few labels + many unlabelled points — common when labelling is costly.'], ['Self-supervised', 'invent labels from the data itself (predict a masked word); the engine of modern pretraining.']],
      table: { cols: ['Paradigm', 'Data', 'Goal', 'Examples'], rows: [
        ['Supervised', 'labelled (x, y)', 'predict y for new x', 'spam filter, price model, image labels'],
        ['Unsupervised', 'unlabelled x', 'discover structure', 'customer segments, PCA, anomaly detection'],
        ['Reinforcement', 'states, actions, rewards', 'maximise long-run reward', 'robotics, game AI, RLHF']] },
      note: 'The paradigms blur: a self-supervised model pretrained on raw text is fine-tuned with supervision and aligned with RL. Knowing which signal you have decides which tools even apply.' },

    { h: 'Linear regression & least squares', p: [
      'Linear regression models a continuous target as a weighted sum of features plus a bias: <code>&#375; = &beta;&#8320; + &beta;&#8321;x&#8321; + &hellip; + &beta;&#8341;x&#8341;</code>. We fit &beta; by <b>ordinary least squares</b> — minimising the sum of squared residuals. Squaring (not absolute value) makes the loss smooth and convex, penalises large errors heavily, and yields a clean closed-form solution.',
      'Setting the gradient of the loss to zero gives the <b>normal equations</b>. In practice we never invert <code>X&#7488;X</code> directly (it is numerically fragile); we let a QR or SVD solver do it — that is exactly what numpy&rsquo;s <code>lstsq</code> and sklearn&rsquo;s <code>LinearRegression</code> use under the hood.',
      'Least squares is also <b>maximum likelihood</b> under the assumption of Gaussian noise — one of the deep bridges between statistics and ML.'],
      eqn: 'minimise  L(β) = Σᵢ (yᵢ − xᵢ·β)² = ‖Xβ − y‖²\nβ̂ = (XᵀX)⁻¹ Xᵀ y      (the normal equations)',
      ex: [{ h: 'Fitting a line to 4 points by hand', p: ['Take points <code>(1,2), (2,3), (3,5), (4,4)</code>. With mean x&#772;=2.5, y&#772;=3.5, the slope is &beta;&#8321; = &Sigma;(x&#8722;x&#772;)(y&#8722;y&#772;) / &Sigma;(x&#8722;x&#772;)&sup2; = 4/5 = 0.8, and the intercept &beta;&#8320; = y&#772; &minus; &beta;&#8321;x&#772; = 3.5 &minus; 0.8&middot;2.5 = 1.5. So the best-fit line is &#375; = 1.5 + 0.8x.'],
        eqn: 'β₁ = Σ(x−x̄)(y−ȳ) / Σ(x−x̄)² = 4 / 5 = 0.8\nβ₀ = ȳ − β₁·x̄ = 3.5 − 0.8·2.5 = 1.5',
        code: 'import numpy as np\nx = np.array([1., 2., 3., 4.]); y = np.array([2., 3., 5., 4.])\nX = np.c_[np.ones(len(x)), x]          # bias column\nbeta = np.linalg.lstsq(X, y, rcond=None)[0]\nbeta        # -> [1.5, 0.8]  (intercept, slope)' }],
      note: 'R² (the coefficient of determination) reports the fraction of variance explained: 1 is perfect, 0 is no better than predicting the mean, and it can go negative on bad fits.' },

    { h: 'Logistic regression & the sigmoid', p: [
      'For <b>classification</b> we need an output in [0,1] we can read as a probability. Logistic regression passes the same linear score <code>z = &beta;&middot;x</code> through the <b>sigmoid</b> &sigma;(z) = 1/(1+e&#8315;&#7767;), an S-curve that squashes any real number into (0,1). The decision boundary is still linear — it is the set where z = 0, i.e. p = 0.5.',
      'We do <i>not</i> use squared error (it is non-convex here); we minimise <b>binary cross-entropy</b> (log loss), which is the negative log-likelihood of the Bernoulli model. There is no closed form, so we fit by gradient-based optimisation. Beautifully, the gradient has the same tidy shape as linear regression: <code>(prediction &minus; label)&middot;x</code>.'],
      eqn: 'σ(z) = 1 / (1 + e⁻ᶻ)          z = β·x\nBCE = −Σ [ y·log(p) + (1−y)·log(1−p) ]\n∂BCE/∂β = Σ (pᵢ − yᵢ)·xᵢ',
      ex: [{ h: 'Turning a score into a probability', p: ['Suppose a fitted model gives <code>z = &beta;&middot;x = 1.1</code> for an email. Then p = &sigma;(1.1) = 1/(1+e&#8315;&#185;&#183;&#185;) = 1/(1+0.333) = 0.750 — a 75% chance of spam, which at the usual 0.5 threshold is classified spam. A borderline z = 0 gives exactly p = 0.5.'],
        eqn: 'σ(1.1) = 1 / (1 + 0.3329) = 0.750',
        code: 'from sklearn.linear_model import LogisticRegression\nclf = LogisticRegression(max_iter=1000).fit(X_train, y_train)\np = clf.predict_proba(X_test)[:, 1]   # P(class = 1)\nyhat = (p >= 0.5).astype(int)         # threshold to a label' }],
      note: 'The multi-class version replaces the sigmoid with the softmax and log loss with categorical cross-entropy — the same model that sits on top of a neural network classifier.' },

    { h: 'Regularization: Ridge (L2) & Lasso (L1)', p: [
      'Unconstrained fitting can drive weights huge to chase noise — <b>overfitting</b>. Regularization adds a penalty on weight size to the loss, trading a little bias for a large drop in variance. <b>Ridge (L2)</b> adds &lambda;&Sigma;&beta;&sup2;, shrinking all weights smoothly toward zero. <b>Lasso (L1)</b> adds &lambda;&Sigma;|&beta;|, whose corner-shaped constraint drives some weights <i>exactly</i> to zero — performing automatic feature selection.',
      'The knob &lambda; controls strength: &lambda;=0 recovers plain least squares; large &lambda; forces a simpler, flatter model. <b>Elastic Net</b> mixes both penalties to get sparsity plus stability when features are correlated. Regularization is why linear models still compete on high-dimensional data — and it applies identically to logistic regression (sklearn&rsquo;s <code>C</code> is the inverse of &lambda;).'],
      eqn: 'Ridge:  L = ‖Xβ − y‖² + λ·Σ βⱼ²       (L2, shrinks)\nLasso:  L = ‖Xβ − y‖² + λ·Σ |βⱼ|      (L1, sparsifies)\nRidge closed form:  β = (XᵀX + λI)⁻¹ Xᵀ y',
      ex: [{ h: 'Lasso zeroing out junk features', p: ['With 100 features where only 5 truly matter, Lasso at a moderate &lambda; sets the 95 useless coefficients to exactly 0, leaving a sparse, interpretable model. Ridge would instead keep all 100 but shrink them small. Always <b>standardise features first</b> — the penalty is scale-sensitive.'],
        code: 'from sklearn.linear_model import Ridge, Lasso\nfrom sklearn.preprocessing import StandardScaler\nXs = StandardScaler().fit_transform(X)      # penalty needs equal scales\nridge = Ridge(alpha=1.0).fit(Xs, y)\nlasso = Lasso(alpha=0.1).fit(Xs, y)\n(lasso.coef_ != 0).sum()   # how many features survived' }],
      note: 'The Ridge form (XᵀX + λI) is always invertible even when XᵀX is singular — regularization fixes numerical as well as statistical problems.' },

    { h: 'k-Nearest Neighbours & distance', p: [
      '<b>k-NN</b> is the simplest useful classifier and has no training phase at all: to predict a new point, find the <code>k</code> closest stored examples and take a majority vote (classification) or an average (regression). All the work is at query time — it is a <i>lazy</i>, non-parametric, instance-based learner.',
      'Two choices dominate behaviour. <b>Distance metric</b>: usually Euclidean (L2), but Manhattan (L1) or cosine for high-dimensional / text data. <b>k</b>: small k gives a jagged, low-bias/high-variance boundary; large k smooths it toward the majority class. Because distances are meaningless when features live on different scales, <b>scaling is mandatory</b>, and k-NN degrades in very high dimensions (the curse of dimensionality).'],
      eqn: 'd₂(a, b) = √( Σᵢ (aᵢ − bᵢ)² )       (Euclidean)\nŷ = majority_vote( k nearest neighbours of x )',
      ex: [{ h: 'Classifying by the 3 nearest points', p: ['A new point sits at distances 0.5, 0.9, 1.1, 2.4 from four training points with labels A, A, B, A. With k=3 the nearest three are labels A, A, B &rarr; vote 2&ndash;1 &rarr; predict <b>A</b>. With k=1 we would trust only the single closest (A). Increasing k to 4 (A,A,B,A) still gives A but by 3&ndash;1.'],
        code: 'from sklearn.neighbors import KNeighborsClassifier\nfrom sklearn.pipeline import make_pipeline\nfrom sklearn.preprocessing import StandardScaler\nknn = make_pipeline(StandardScaler(),\n                    KNeighborsClassifier(n_neighbors=3))\nknn.fit(X_train, y_train)\nknn.score(X_test, y_test)' }],
      note: 'Prediction cost grows with the dataset; ball-tree / KD-tree indexes speed up neighbour search, but k-NN is best for small-to-medium data.' },

    { h: 'Support Vector Machines & the kernel trick', p: [
      'An <b>SVM</b> finds the linear boundary with the <b>widest margin</b> — the biggest gap between the two classes. Only the points on the margin&rsquo;s edge, the <b>support vectors</b>, determine the boundary; everything else is irrelevant. Maximising the margin is a convex quadratic program with strong generalisation guarantees. A <i>soft margin</i> (the <code>C</code> parameter) allows some violations so noisy data stays solvable — small C means a wide, tolerant margin; large C means a hard, low-error fit.',
      'The <b>kernel trick</b> is the magic: replace every dot product x&middot;x&prime; with a kernel K(x, x&prime;) that equals a dot product in some high-dimensional feature space — <i>without ever computing coordinates there</i>. This lets a linear algorithm draw curved boundaries. The RBF (Gaussian) kernel maps into an infinite-dimensional space and is the default nonlinear choice.'],
      eqn: 'maximise margin  ⇔  minimise ½‖w‖²  s.t.  yᵢ(w·xᵢ + b) ≥ 1\nRBF kernel:  K(x, x′) = exp( −γ·‖x − x′‖² )',
      ex: [{ h: 'Why the kernel trick saves work', p: ['A degree-2 polynomial kernel <code>K(x,x′) = (x&middot;x′ + 1)&sup2;</code> corresponds to a feature space containing all pairwise products x&#7522;x&#11388;. For 1000 features that explicit space has ~500,000 dimensions — but computing K is just one dot product plus a square. We get the expressive power of the huge space at the price of the small one.'],
        code: 'from sklearn.svm import SVC\n# RBF kernel: gamma sets how far one example\'s influence reaches\nsvm = SVC(kernel=\'rbf\', C=1.0, gamma=\'scale\').fit(X_train, y_train)\nlen(svm.support_)         # number of support vectors\nsvm.score(X_test, y_test)' }],
      note: 'SVMs shine on medium-sized, high-dimensional problems (e.g. text) but scale poorly past ~100k rows; use linear SVMs or SGD for larger data.' },

    { h: 'Decision trees & ensembles', p: [
      'A <b>decision tree</b> asks a sequence of yes/no questions about features, recursively splitting the data until each leaf is (nearly) pure. At every node it greedily picks the feature and threshold that most reduce impurity, measured by <b>Gini</b> = 1 &minus; &Sigma;p&#7522;&sup2; or <b>entropy</b> = &minus;&Sigma;p&#7522;log&#8322;p&#7522;. The chosen split maximises <b>information gain</b> — the parent&rsquo;s impurity minus the weighted average impurity of the children. Trees need no scaling and are wildly interpretable, but a fully grown tree memorises the training set (high variance); we tame it by limiting depth or samples-per-leaf.',
      'That very instability is what <b>ensembles</b> exploit. <b>Bagging</b> trains many trees on random bootstraps <i>in parallel</i> and averages them, slashing variance; a <b>Random Forest</b> adds random feature subsets per split to decorrelate the trees further. <b>Boosting</b> instead trains trees <i>sequentially</i>, each fixing the last one&rsquo;s mistakes, which reduces bias: <b>AdaBoost</b> reweights misclassified points, <b>Gradient Boosting</b> fits each tree to the residual (negative gradient) of the loss, and <b>XGBoost / LightGBM</b> are regularised, highly optimised gradient boosters that dominate tabular competitions.'],
      eqn: 'Gini(node) = 1 − Σ pᵢ²       Entropy(node) = −Σ pᵢ log₂ pᵢ\nGain = Impurity(parent) − Σ (nₖ/n)·Impurity(childₖ)',
      ex: [
        { h: 'Scoring one split with Gini', p: ['A node with 10 samples (6 positive, 4 negative) has Gini = 1 &minus; (0.6&sup2; + 0.4&sup2;) = 0.48. A candidate split makes a pure left child (4 pos, 0 neg &rarr; Gini 0) and a right child (2 pos, 4 neg &rarr; Gini 1 &minus; (0.333&sup2;+0.667&sup2;) = 0.444). Weighted child impurity = (4/10)&middot;0 + (6/10)&middot;0.444 = 0.267, so information gain = 0.48 &minus; 0.267 = <b>0.213</b>. The tree keeps the split with the highest gain.'],
          eqn: 'parent Gini = 1 − (.6² + .4²) = 0.48\nweighted children = .4·0 + .6·0.444 = 0.267\ngain = 0.48 − 0.267 = 0.213' },
        { h: 'Random Forest vs a single tree', p: ['On noisy data a lone tree might hit 82% test accuracy while overfitting (98% train). A 300-tree Random Forest, averaging away the variance, often reaches ~90% test with a far smaller train/test gap — no scaling and little tuning. Gradient boosting can push higher still but needs a tuned learning rate and early stopping to avoid overfitting.'],
          code: 'from sklearn.ensemble import (RandomForestClassifier,\n    GradientBoostingClassifier)\nrf = RandomForestClassifier(n_estimators=300, max_features=\'sqrt\',\n            n_jobs=-1, random_state=0).fit(X_train, y_train)\ngb = GradientBoostingClassifier(n_estimators=200, learning_rate=0.05,\n            max_depth=3).fit(X_train, y_train)\nrf.score(X_test, y_test), gb.score(X_test, y_test)' }],
      table: { cols: ['Method', 'Trees built', 'Reduces', 'Signature'], rows: [
        ['Bagging', 'parallel, on bootstraps', 'variance', 'average of independent models'],
        ['Random Forest', 'parallel + feature subsampling', 'variance', 'decorrelated trees, robust default'],
        ['AdaBoost', 'sequential', 'bias', 'reweight the errors'],
        ['Gradient Boosting', 'sequential, on residuals', 'bias', 'fit the negative gradient'],
        ['XGBoost / LightGBM', 'sequential, regularised', 'bias + variance', 'fast, shrinkage, early stopping']] },
      note: 'Rule of thumb: a Random Forest is a strong, low-effort baseline; reach for gradient boosting (XGBoost/LightGBM) when you want the last few points and can afford to tune. Regression trees split on variance (MSE) reduction and predict the leaf mean.' },

    { h: 'Naive Bayes', p: [
      '<b>Naive Bayes</b> applies Bayes&rsquo; theorem with one bold simplifying assumption: that features are <b>conditionally independent</b> given the class. That assumption is almost never true, yet the classifier is fast, needs little data, and is remarkably strong for text — spam filtering and document classification are its classic wins. It picks the class that maximises the posterior, which by Bayes is proportional to the prior times the product of per-feature likelihoods.',
      'Variants match the feature type: <b>Multinomial NB</b> for word counts, <b>Bernoulli NB</b> for binary presence, <b>Gaussian NB</b> for continuous features. We add <b>Laplace smoothing</b> so an unseen word doesn&rsquo;t zero out the whole product, and we sum <i>log</i>-probabilities to avoid numerical underflow.'],
      eqn: 'P(class | x) ∝ P(class) · ∏ⱼ P(xⱼ | class)\nŷ = argmax_c [ log P(c) + Σⱼ log P(xⱼ | c) ]',
      ex: [{ h: 'Spam by word probabilities', p: ['Suppose P(spam)=0.4 and, given spam, P(&ldquo;free&rdquo;)=0.8, P(&ldquo;win&rdquo;)=0.6; given ham, P(&ldquo;free&rdquo;)=0.1, P(&ldquo;win&rdquo;)=0.05 with P(ham)=0.6. For an email with both words: spam score = 0.4&middot;0.8&middot;0.6 = 0.192; ham score = 0.6&middot;0.1&middot;0.05 = 0.003. Spam wins by ~64&times; &rarr; classify <b>spam</b> (posterior 0.192/0.195 &asymp; 0.985).'],
        eqn: 'spam ∝ 0.4·0.8·0.6 = 0.192\nham  ∝ 0.6·0.1·0.05 = 0.003\nP(spam|x) = 0.192 / (0.192+0.003) ≈ 0.985',
        code: 'from sklearn.feature_extraction.text import CountVectorizer\nfrom sklearn.naive_bayes import MultinomialNB\nfrom sklearn.pipeline import make_pipeline\nmodel = make_pipeline(CountVectorizer(), MultinomialNB(alpha=1.0))  # Laplace\nmodel.fit(train_texts, train_labels)\nmodel.predict([\'win a free prize now\'])' }],
      note: 'Naive Bayes is often poorly calibrated (its probabilities are over-confident because of the independence assumption) even when its rankings and decisions are good.' },

    { h: 'Clustering & choosing k', p: [
      'Clustering groups unlabelled points by similarity. <b>K-Means</b> partitions data into <code>k</code> clusters by alternating until stable: (1) <i>assign</i> each point to its nearest centroid, (2) <i>update</i> each centroid to the mean of its points. It minimises within-cluster squared distance (inertia), is fast, but assumes roughly spherical, equal-sized clusters and needs k up front — initialise with <b>k-means++</b> to avoid poor local minima. <b>Hierarchical (agglomerative)</b> clustering builds a dendrogram by repeatedly merging the two closest clusters (no k needed; cut the tree where you like). <b>DBSCAN</b> grows clusters from dense regions — points with &ge; <code>minPts</code> neighbours within radius <code>&epsilon;</code> are cores — finding arbitrary shapes and labelling sparse points as <b>noise</b>.',
      'Because K-Means needs k up front, we pick it by scanning values. The <b>elbow method</b> plots inertia against k and looks for where the steep drop suddenly flattens. The sharper <b>silhouette score</b> compares each point&rsquo;s mean intra-cluster distance <code>a</code> with its distance <code>b</code> to the nearest other cluster: s = (b&minus;a)/max(a,b) in [&minus;1, 1], where ~1 is tight and well-separated, ~0 is on a boundary, and negative means mis-assigned. Choose the k that maximises the average silhouette.'],
      eqn: 'K-Means:  minimise Σₖ Σ_{x∈Cₖ} ‖x − μₖ‖²  ⇒  assign x→nearest μₖ ; μₖ←mean(Cₖ)\nsilhouette(i) = (bᵢ − aᵢ) / max(aᵢ, bᵢ)   ∈ [−1, 1]',
      ex: [
        { h: 'One K-Means iteration', p: ['Points 1, 2, 3, 10, 11, 12 on a line with initial centroids at 2 and 11. <b>Assign</b>: {1,2,3} go to centroid 2, {10,11,12} to centroid 11. <b>Update</b>: new centroids become mean(1,2,3)=2 and mean(10,11,12)=11 — already stable, so it has converged to the two obvious clusters. DBSCAN with &epsilon;=1.5, minPts=2 finds the same two dense groups and flags any lone far-away point as noise.'],
          code: 'from sklearn.cluster import KMeans, DBSCAN\nfrom sklearn.preprocessing import StandardScaler\nXs = StandardScaler().fit_transform(X)\nkm = KMeans(n_clusters=3, init=\'k-means++\', n_init=10).fit(Xs)\nkm.labels_, km.inertia_        # cluster ids, within-cluster SSE\ndb = DBSCAN(eps=0.5, min_samples=5).fit(Xs)\n(db.labels_ == -1).sum()       # points labelled noise' },
        { h: 'Reading the elbow and silhouette', p: ['Say inertia for k = 1..6 is 900, 420, 210, 180, 165, 150 — the big drops stop after k=3 (the elbow). Silhouette for k = 2,3,4 is 0.55, 0.68, 0.52 — also peaking at k=3. Both agree, so choose <b>k = 3</b>. When they disagree, trust the silhouette and domain knowledge over the elbow&rsquo;s eyeballing.'],
          code: 'from sklearn.metrics import silhouette_score\nfor k in range(2, 7):\n    km = KMeans(n_clusters=k, n_init=10).fit(Xs)\n    print(k, round(km.inertia_, 1),\n          round(silhouette_score(Xs, km.labels_), 3))' }],
      note: 'K-Means and DBSCAN distances are scale-sensitive — always standardise first. Tune DBSCAN\'s eps with a k-distance plot rather than guessing.' },

    { h: 'Dimensionality reduction: PCA, t-SNE, UMAP', p: [
      'High-dimensional data is hard to model and impossible to eyeball. <b>PCA</b> is the linear workhorse: it finds orthogonal directions (the eigenvectors of the covariance matrix, or the right singular vectors via SVD) that capture maximal variance, then keeps the top few. It is fast, deterministic and reversible, and the <b>explained-variance ratio</b> tells you how much information each component retains. Use it to denoise, compress and speed up downstream models.',
      '<b>t-SNE</b> and <b>UMAP</b> are <i>nonlinear</i> methods built for <b>visualisation</b>: they preserve local neighbourhood structure so clusters become visible in 2-D. t-SNE is beautiful but slow, non-deterministic and distorts global distances; UMAP is faster, scales better and preserves more global structure. Crucial caveat: distances and cluster sizes in a t-SNE/UMAP plot are <b>not</b> quantitatively meaningful — never read them as real geometry.'],
      table: { cols: ['Method', 'Type', 'Best for', 'Watch out'], rows: [
        ['PCA', 'linear', 'compression, denoising, preprocessing', 'misses nonlinear structure'],
        ['t-SNE', 'nonlinear', 'local-structure visualisation', 'slow, stochastic, no global distances'],
        ['UMAP', 'nonlinear', 'fast visualisation + some global structure', 'hyperparameter-sensitive']] },
      ex: [{ h: 'PCA that keeps 95% of the variance', p: ['On 784-pixel MNIST images, PCA often needs only ~150 components to retain 95% of the variance — a 5&times; compression that speeds up any downstream classifier with little accuracy loss. Choose the component count from the cumulative explained-variance curve rather than guessing.'],
        code: 'from sklearn.decomposition import PCA\npca = PCA(n_components=0.95).fit(X)   # keep 95% of variance\nZ = pca.transform(X)\nprint(pca.n_components_, Z.shape)     # e.g. 150 components\n# for a 2-D picture instead:\nfrom sklearn.manifold import TSNE\nemb = TSNE(n_components=2, perplexity=30, init=\'pca\').fit_transform(X[:2000])' }],
      note: 'Standardise before PCA so a large-scale feature doesn’t dominate the variance purely because of its units.' },

    { h: 'Evaluation metrics: confusion matrix to AUC', p: [
      'Accuracy alone lies on imbalanced data — a model that always predicts &ldquo;not fraud&rdquo; scores 99.5% while catching zero fraud. Start from the <b>confusion matrix</b> of TP / FP / FN / TN and derive: <b>precision</b> = TP/(TP+FP) (of those flagged positive, how many really are) and <b>recall</b> = TP/(TP+FN) (of the real positives, how many we caught). The <b>F1</b> score is their harmonic mean, rewarding a balance of both.',
      'A classifier outputs probabilities, so its quality depends on the <b>threshold</b>. The <b>ROC curve</b> plots TPR vs FPR across all thresholds; <b>ROC-AUC</b> is the area under it — the probability the model ranks a random positive above a random negative (0.5 = coin flip, 1.0 = perfect). On <b>heavily imbalanced</b> data prefer the <b>PR curve / PR-AUC</b>, which focuses on the rare positive class and is not flattered by the huge true-negative count.'],
      eqn: 'precision = TP/(TP+FP)     recall = TP/(TP+FN)\nF1 = 2·precision·recall / (precision + recall)\nTPR = TP/(TP+FN)     FPR = FP/(FP+TN)',
      ex: [{ h: 'Scoring a fraud model', p: ['Out of 1000 transactions, 50 are fraud. The model flags 60 as fraud: 40 are truly fraud (TP), 20 are false alarms (FP); it misses 10 frauds (FN); the other 930 are correct negatives (TN). Then precision = 40/60 = 0.667, recall = 40/50 = 0.800, and F1 = 2&middot;0.667&middot;0.8/(0.667+0.8) = <b>0.727</b>. Accuracy is (40+930)/1000 = 0.970 — impressively high yet it hides that 1 in 5 frauds slipped through.'],
        eqn: 'precision = 40/60 = 0.667\nrecall    = 40/50 = 0.800\nF1 = 2·(0.667·0.800)/(0.667+0.800) = 0.727',
        code: 'from sklearn.metrics import (classification_report,\n    confusion_matrix, roc_auc_score, average_precision_score)\np = clf.predict_proba(X_test)[:, 1]\nyhat = (p >= 0.5).astype(int)\nprint(confusion_matrix(y_test, yhat))\nprint(classification_report(y_test, yhat))\nprint(\'ROC-AUC\', roc_auc_score(y_test, p))\nprint(\'PR-AUC \', average_precision_score(y_test, p))  # better when imbalanced' }],
      note: 'Choose the threshold to fit the cost of errors: a cancer screen wants high recall (miss nothing), a spam filter wants high precision (never bin a real email).' },

    { h: 'Cross-validation & the bias–variance tradeoff', p: [
      'The cardinal sin of ML is judging a model on data it trained on — that measures memorisation, not generalisation. The discipline: a <b>training</b> set fits parameters, a <b>validation</b> set tunes hyperparameters and chooses models, and a <b>test</b> set is touched exactly once, at the very end. <b>k-fold cross-validation</b> squeezes more signal from limited data: split into k folds, train on k&minus;1 and validate on the held-out fold, rotate through all k, and average — every point serves as both training and validation. Use <b>stratified</b> folds to preserve class ratios, and forward-chaining splits for time series so you never train on the future.',
      'What CV reveals is the <b>bias–variance tradeoff</b>. Error decomposes into <b>bias</b> (too-simple assumptions), <b>variance</b> (over-sensitivity to the training sample), and irreducible <b>noise</b>. <b>Underfitting</b> = high bias: poor on both train and test. <b>Overfitting</b> = high variance: near-perfect on train, poor on test. You diagnose from the train&ndash;validation gap and move along the curve with model complexity, regularization, and — the one free lunch — <b>more data</b>, which cuts variance without adding bias.'],
      eqn: 'CV score = (1/k)·Σᵢ metric(foldᵢ)          typically k = 5 or 10\nExpected error = Bias² + Variance + Irreducible noise',
      ex: [
        { h: '5-fold CV for honest tuning', p: ['With 1000 rows, 5-fold CV trains on 800 and validates on 200, five times, so all 1000 rows serve as validation exactly once. Reporting the mean &plusmn; std across folds (say 0.86 &plusmn; 0.02) is far more trustworthy than a single lucky split — and the std warns you how stable the model is.'],
          code: 'from sklearn.model_selection import cross_val_score, StratifiedKFold\ncv = StratifiedKFold(n_splits=5, shuffle=True, random_state=0)\nscores = cross_val_score(clf, X_train, y_train, cv=cv, scoring=\'f1\')\nprint(f\'F1 = {scores.mean():.3f} +/- {scores.std():.3f}\')\n# the held-out test set stays untouched until the very end' },
        { h: 'Reading a validation curve', p: ['Fit trees of increasing depth to the same data. Shallow trees underfit (train and val error both high — bias). Very deep trees overfit: train error &rarr; ~0 but val error climbs — the tell-tale gap of high variance. The best model minimises <i>validation</i> error, not training error.'],
          code: 'import numpy as np\nfrom sklearn.model_selection import validation_curve\nfrom sklearn.tree import DecisionTreeClassifier\ndepths = range(1, 20)\ntr, va = validation_curve(DecisionTreeClassifier(), X, y,\n            param_name=\'max_depth\', param_range=depths, cv=5)\nbest = depths[int(np.argmax(va.mean(axis=1)))]  # depth with best val score' }],
      table: { cols: ['Symptom', 'Train err', 'Val err', 'Diagnosis', 'Fix'], rows: [
        ['Underfit', 'high', 'high', 'high bias / too simple', 'more features, less regularization, richer model'],
        ['Good fit', 'low', 'low', 'balanced', 'ship it'],
        ['Overfit', 'very low', 'high', 'high variance / too complex', 'more data, regularization, simpler model, early stopping']] },
      note: 'Any preprocessing that learns from data (scaling, imputation, feature selection) must live INSIDE the CV loop, fit on training folds only — otherwise you leak information and over-report accuracy. Ensembles game the tradeoff: bagging attacks variance, boosting attacks bias.' },

    { h: 'Feature engineering, imbalance & scikit-learn pipelines', p: [
      'For classical ML, <b>features usually matter more than the model</b>. Good <b>feature engineering</b> — ratios, interactions, date parts, domain aggregates, text vectorised with TF-IDF — turns a mediocre model into a great one. Then two mechanical steps decide whether it even works. <b>Scaling</b>: distance- and gradient-based models (k-NN, SVM, linear, K-Means, PCA) need features on comparable scales via <code>StandardScaler</code> or <code>MinMaxScaler</code>; tree ensembles are scale-invariant. <b>Encoding</b>: one-hot for low-cardinality nominal features, ordinal for ordered ones, target/frequency encoding for high-cardinality. And for <b>imbalanced data</b> (fraud, disease, churn), resample the minority up (SMOTE) or the majority down, use <b>class weights</b>, shift the decision threshold, and evaluate with PR-AUC / F1 rather than accuracy.',
      'A <b>pipeline</b> chains all of this preprocessing and the model into one object with a single <code>fit</code> / <code>predict</code>. This is not a convenience — it is a <b>correctness</b> guarantee: during cross-validation and grid search every transform is re-fit on the training folds only, so no test information ever leaks through the scaler, imputer or resampler. A <b>ColumnTransformer</b> routes numeric and categorical columns through different preprocessing, and <b>GridSearchCV</b> tunes the whole chain by CV. The mature loop: build a pipeline &rarr; cross-validate to compare models &rarr; grid-search the winner &rarr; refit on all training data &rarr; evaluate <b>once</b> on the held-out test set.'],
      terms: [['StandardScaler', 'z = (x − μ)/σ — zero mean, unit variance; the default.'], ['MinMaxScaler', 'rescales to [0,1]; good when you need bounded inputs.'], ['One-hot encoding', 'one binary column per category; imposes no false ordering.'], ['SMOTE', 'synthesise minority-class points by interpolating neighbours.'], ['class_weight', 'penalise minority errors more so the model attends to them.'], ['TF-IDF', 'weight words by frequency in a doc vs rarity across the corpus.']],
      ex: [
        { h: 'Rebalancing a 1:99 problem', p: ['With 1% positives, set <code>class_weight=\'balanced\'</code> so each positive error counts ~99&times; a negative one, or apply SMOTE to lift the minority to a healthier ratio. Fit SMOTE <b>only on the training folds</b> — never resample validation or test data, or your scores become fiction.'],
          code: 'from imblearn.over_sampling import SMOTE\nfrom imblearn.pipeline import Pipeline    # SMOTE-aware pipeline\nfrom sklearn.linear_model import LogisticRegression\npipe = Pipeline([\n    (\'smote\', SMOTE(random_state=0)),     # applied to train folds only\n    (\'clf\', LogisticRegression(class_weight=\'balanced\', max_iter=1000)),\n])\npipe.fit(X_train, y_train)' },
        { h: 'A leak-proof end-to-end pipeline', p: ['Numeric columns get imputed and scaled; categorical columns get imputed and one-hot encoded; both feed a gradient-boosting classifier — all tuned by 5-fold CV. Because the preprocessing lives inside the pipeline, <code>GridSearchCV</code> re-fits it on each fold with zero leakage, and the best model is ready to deploy as one object.'],
          code: 'from sklearn.compose import ColumnTransformer\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.impute import SimpleImputer\nfrom sklearn.preprocessing import StandardScaler, OneHotEncoder\nfrom sklearn.ensemble import GradientBoostingClassifier\nfrom sklearn.model_selection import GridSearchCV\n\nnum = [\'age\', \'income\']; cat = [\'country\', \'plan\']\npre = ColumnTransformer([\n    (\'num\', Pipeline([(\'imp\', SimpleImputer(strategy=\'median\')),\n                      (\'sc\', StandardScaler())]), num),\n    (\'cat\', Pipeline([(\'imp\', SimpleImputer(strategy=\'most_frequent\')),\n                      (\'oh\', OneHotEncoder(handle_unknown=\'ignore\'))]), cat),\n])\npipe = Pipeline([(\'pre\', pre), (\'clf\', GradientBoostingClassifier())])\ngrid = {\'clf__n_estimators\': [100, 300], \'clf__learning_rate\': [0.05, 0.1]}\nsearch = GridSearchCV(pipe, grid, cv=5, scoring=\'f1\', n_jobs=-1)\nsearch.fit(X_train, y_train)     # preprocessing re-fit per fold\nprint(search.best_params_, search.best_score_)\nsearch.score(X_test, y_test)     # touch the test set ONCE' }],
      note: 'Never fit a scaler or SMOTE on the full dataset before splitting — that leaks test information. A saved pipeline (joblib.dump) captures preprocessing + model together, so training and production apply identical transforms — the biggest guard against train/serve skew.' },
  ]
});
